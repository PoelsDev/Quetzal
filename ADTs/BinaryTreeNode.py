class BinaryTreeNode:
    def __init__(self, searchKey):
        """
        +BinaryTreeNode(in searchKey: KeyType)
        The object binary tree node that has as attribute a searchKey which will be given when node is created
        :param searchKey: The object that you want your node to contain as an attribute
        """
        self.searchKey = searchKey

